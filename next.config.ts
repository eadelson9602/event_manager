import type { NextConfig } from "next";

/**
 * Configuración de Next.js
 * 
 * Nota: El puerto de la aplicación se configura mediante la variable de entorno PORT
 * en los scripts de package.json. Si no se define, se usa el puerto por defecto (3000).
 * 
 * Ejemplo de uso:
 * - Windows: set PORT=3001 && npm run dev
 * - Linux/Mac: PORT=3001 npm run dev
 * - O simplemente: npm run dev (usa puerto 3000 por defecto)
 */
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
